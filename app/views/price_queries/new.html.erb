

<div class="col-xs-12 col-sm-6 col-sm-3-offset col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3 price-queries">
	<h2 id="price_queries-h2">Add an Address</h2>
	<div class="price-queries-form">
						
		<% flash.each do |k, v| %>
		  <h4 id="<%= k %>"><%= v %></h4>
		<% end %>
	<%= render partial: "form", locals: {user: @user, price_query: @price_query} %>
	</div>
</div>		
<div class="col-md-2"></div>
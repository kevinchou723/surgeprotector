<div class="col-xs-12 col-sm-6 col-sm-3-offset col-md-6 col-md-offset-3 col-lg-6 col-lg-offset-3 edit-profile">
	<h2 id="edit-profile-h2">Edit Profile</h2>
	<div class="edit-profile-form">
						
		<% flash.each do |k, v| %>
		  <h4 id="<%= k %>"><%= v %></h4>
		<% end %>

		<%= render partial: "form", locals: {user: @user} %>
	</div>
</div>		
<div class="col-md-2"></div>